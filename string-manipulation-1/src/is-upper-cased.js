/* exported isUpperCased */

// define a function isUpperCased with one parameter word
// return toUpperCase() method of the word object is strictly equal to word

function isUpperCased(word) {
  return word.toUpperCase() === word;
}
