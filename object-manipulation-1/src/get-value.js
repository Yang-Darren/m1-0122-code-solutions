/* exported getValue */
// define a function getValue with two parameters object, and key
// the key method of the object, object is return in bracket notation

function getValue(object, key) {
  return object[key];
}
